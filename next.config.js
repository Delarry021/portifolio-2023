/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "https://apexensino.com.br",
      "apexensino.com.br",
    ],
  },
};

module.exports = nextConfig;
